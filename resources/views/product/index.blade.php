@extends('layouts.app') @section('content')<div class="admin-title row"><div><span>CATALOG</span><h1>Продукти</h1><p>Двомовний каталог мікродобрив.</p></div><a class="admin-btn" href="{{ route('products.create') }}">+ Новий продукт</a></div><div class="admin-panel"><table><thead><tr><th>Назва</th><th>Категорії</th><th>Фасування</th><th>Статус</th><th></th></tr></thead><tbody>@foreach($products as $product)<tr><td><b>{{ $product->title_uk }}</b><small>{{ $product->subtitle_uk }}</small></td><td>{{ $product->categories->pluck('name_uk')->join(', ') }}</td><td>{{ $product->volume }}</td><td><span class="status {{ $product->is_active?'live':'' }}">{{ $product->is_active?'Опубліковано':'Чернетка' }}</span></td><td class="actions"><a href="{{ route('products.edit',$product) }}">Редагувати</a><form method="POST" action="{{ route('products.destroy',$product) }}" onsubmit="return confirm('Видалити продукт?')">@csrf @method('DELETE')<button>×</button></form></td></tr>@endforeach</tbody></table></div>@endsection
