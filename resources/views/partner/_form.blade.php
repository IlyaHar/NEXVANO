<div class="form-card"><div class="fields two"><label>Назва<input name="name" value="{{ old('name',$partner?->name) }}" required></label><label>Логотип<input type="file" name="logo" accept="image/*" {{ $partner?'':'required' }}>@if($partner?->logo)<img class="admin-logo preview" src="{{ asset('storage/partners/'.$partner->logo) }}">@endif</label><label>Сайт<input type="url" name="website_url" value="{{ old('website_url',$partner?->website_url) }}"></label><label>Порядок<input type="number" name="sort_order" value="{{ old('sort_order',$partner?->sort_order ?? 0) }}"></label><label>Опис<textarea name="description" rows="4">{{ old('description',$partner?->description) }}</textarea></label></div><div class="checks"><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$partner?->is_active ?? true))> Показувати на сайті</label></div></div><button class="admin-btn">{{ $submitLabel }}</button>
