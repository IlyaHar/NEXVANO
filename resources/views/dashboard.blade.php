@extends('layouts.app') @section('content')<div class="admin-title"><div><span>DASHBOARD</span><h1>Добрий день, {{ auth()->user()->name }}</h1><p>Керуйте каталогом Nexvano в одному місці.</p></div></div><div class="stats">@foreach([['Продукти',$stats['products'],'products.index'],['Категорії',$stats['categories'],'categories.index'],['Партнери',$stats['partners'],'partners.index']] as $stat)<a href="{{ route($stat[2]) }}"><span>{{ $stat[0] }}</span><strong>{{ $stat[1] }}</strong><i>↗</i></a>@endforeach</div><div class="admin-panel"><div class="panel-head"><h2>Останні продукти</h2><div><a href="{{ route('products.create') }}">+ Новий товар</a> · <a href="{{ route('partners.create') }}">Додати партнера</a></div></div><table><thead><tr><th>Назва</th><th>Категорія</th><th>Статус</th><th></th></tr></thead><tbody>@foreach($recentProducts as $product)<tr><td><b>{{ $product->title_uk }}</b><small>{{ $product->title_es }}</small></td><td>{{ $product->categories->first()?->name_uk }}</td><td><span class="status {{ $product->is_active?'live':'' }}">{{ $product->is_active?'Опубліковано':'Чернетка' }}</span></td><td><a href="{{ route('products.edit',$product) }}">Редагувати</a></td></tr>@endforeach</tbody></table></div>@endsection
